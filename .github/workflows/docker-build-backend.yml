name: 🏗️ Build & Push Web UI (Multi-Arch, Cached)

on:
  push:
    branches: [ "main" ]
    paths:
      - "frontend/**"
      - ".github/workflows/docker-build.yml"
  workflow_dispatch:

jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - name: 🧰 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔑 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🧱 Build & Push (with caching)
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: nate8727/spokenarr-web:latest
          platforms: linux/amd64,linux/arm64
          cache-from: type=registry,ref=nate8727/spokenarr-web:cache
          cache-to: type=registry,ref=nate8727/spokenarr-web:cache,mode=max
