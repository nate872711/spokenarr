# -------------------------------
# 🌐 SPOKENARR FRONTEND CONFIGURATION
# -------------------------------

# Vite environment variables must start with VITE_
VITE_API_BASE_URL=http://localhost:8000/api

# Build & environment
VITE_APP_ENV=production
VITE_APP_NAME=Spokenarr Web UI

# Default ports (Docker Compose maps to 80)
VITE_PORT=5173

# Fallback assets (used by Home.tsx if missing)
VITE_DEFAULT_LOGO=/favicon.ico
VITE_SPINNER_PATH=/assets/spinner.png
