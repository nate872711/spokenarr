# ------------------------------
# 🌍 Global Environment Settings
# ------------------------------

# Project name (used for image tags & compose services)
PROJECT_NAME=spokenarr

# ------------------------------
# 🐳 Backend API Configuration
# ------------------------------

# The hostname used by the frontend proxy (nginx) to reach the backend API
API_HOST=spokenarr-api
API_PORT=8000

# FastAPI settings
APP_ENV=production
LOG_LEVEL=info

# ------------------------------
# 🗄️ Database Configuration
# ------------------------------

# Postgres service credentials
POSTGRES_USER=spokenarr
POSTGRES_PASSWORD=spokenarr
POSTGRES_DB=spokenarr
POSTGRES_HOST=spokenarr-db
POSTGRES_PORT=5432

# SQLAlchemy Database URL for async connection
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ------------------------------
# 🎧 Audio Files & Storage
# ------------------------------

# Path inside the backend container for audio files
AUDIO_PATH=/app/audio

# Local volume mount path for persistent data (optional)
DATA_PATH=./data

# ------------------------------
# 🌐 Frontend Configuration
# ------------------------------

# Public URL for the web app (useful for workflows)
PUBLIC_URL=http://localhost

# Optional Storybook build toggle
SKIP_STORYBOOK=true

# ------------------------------
# 🧰 Docker Build & GitHub Actions
# ------------------------------

# DockerHub Repository (adjust to your username)
DOCKERHUB_USER=nathancopeland
DOCKERHUB_REPO=spokenarr

# Multi-architecture build platforms
BUILD_PLATFORMS=linux/amd64,linux/arm64

# Git tag prefix for automated versioning
TAG_PREFIX=v
